---
description: Schema sync discipline across migrations, types, and API contracts
---

# Schema Sync

- When the data model changes, update every documented sync layer.
- Prefer checked-in migrations over ad hoc edits.
- Run contract/parity checks before calling schema work complete.
