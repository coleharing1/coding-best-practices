description = "Synthesize a single final owner-tagged checklist from the active plans."

prompt = """
# Task: Final Plan Checklist

Read `plans/MACRO-ROADMAP.md` and the active planning artifacts, then synthesize one `plans/active/Plan-XXX-Final.md`.

Rules:
- the Final plan is the implementation source of truth
- resolve contradictions explicitly
- use owner tags such as `[Cole]`, `[Codex]`, `[Claude]`, `[Gemini]`, `[Shared]`, and `[Gate]`
- keep browser/login handoff steps explicit
- include binary verification targets for each phase
- include `Macro Roadmap Alignment` in the Final plan
- update the macro roadmap if synthesis changes long-range sequencing, likely-next work, or roadmap status
- do not write implementation code
"""
